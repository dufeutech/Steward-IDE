# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The bootstrap pack's payload is committed source, not a build output: it is
# first-party, has no build step, and a recovery surface that must be reconstructed
# before it works is not a recovery surface.
!src-tauri/packs-baseline/bootstrap/dist/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
